#include "Rafle.h"
#include"Case.h"
#include"Joueur.h"
#include"piece.h"
#include "Plateau.h"
/*
int Rafle::calculRaflesPossible(Plateau P)
{
    Rafle cases_suivantes1 [100]; int cmp_cs = 0;
    int l=0 , c =1 ,prise=0;
    if(Plateau.Damier[0][1]!=NULL)
    {
        if (P.Damier[l+1][c+1]==NULL)
                {
                    //deplacer
                    cases_suivantes1 [cmp_cs].colonne=c;
                    cases_suivantes1 [cmp_cs].ligne=l;
                    cases_suivantes1 [cmp_cs].nbr_prise=prise;
                    cases_suivantes1 [cmp_cs].caseDebut.Case(l,c+1);

                }
    }
return 0 ;
}
*/
